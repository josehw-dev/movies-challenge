import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'white',
    borderRadius: 20
  },
  focus: {
    height: 1, width: 40, backgroundColor: 'black'
  }
});

export default styles;
